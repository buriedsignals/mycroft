# Mycroft install set — the directories a runtime needs to run the skills.
# Root files (AGENTS.md, LICENSE) are always included by git cone-mode sparse
# checkout, so only directories are listed.
#
#   git sparse-checkout set $(grep -v '^#' install-set.txt)
#
# Not listed: catalog/ install/ (Engine-owned), assets/ fonts/ archive/ docs/ (site
# and documentation).
skills
scripts
tools
instructions
recipes
extensions
.claude-plugin
